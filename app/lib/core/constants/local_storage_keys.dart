/// Constants for local storage keys used throughout the application.
///
/// This class contains all the key strings used for local storage operations.
/// Having them in one place makes it easier to manage and avoid duplication.
class LocalStorageKeys {
  LocalStorageKeys._();

  /// The key for storing the API authentication token.
  static const String apiToken = 'api_token';
}
